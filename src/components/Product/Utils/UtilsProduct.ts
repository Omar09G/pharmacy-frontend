export type ProductRequest = {
  productId: number;
  productName: string;
  productCatalog: number;
  productCount: number;
  productCodeBar: string;
  productPrice: number;
  productDesc: string;
  productLote: string;
};
